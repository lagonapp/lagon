{
  "name": "@lagon/runtime",
  "version": "0.1.0",
  "description": "JavaScript Serverless Runtime for Lagon",
  "private": true,
  "scripts": {
    "build": "cargo build",
    "build:runtime": "pnpm esbuild js/index.ts --bundle --outfile=js/runtime.js --format=esm --target=es2022"
  },
  "devDependencies": {
    "esbuild": "^0.14.41"
  },
  "dependencies": {
    "web-streams-polyfill": "^3.2.1"
  }
}
