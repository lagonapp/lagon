---
description: The latest updates and releases from the Turbo team at Vercel.
type: posts
# This file was mostly inspired by SWR's blog page:
# https://github.com/vercel/swr-site/blob/main/components/blog-index.js
---

import { getPagesUnderRoute } from 'nextra/context';
import Link from 'next/link';

export function BlogIndex() {
  return getPagesUnderRoute('/blog').map(page => {
    return (
      <div className="flex flex-col gap-2">
        <Link href={page.route} className="font-semibold text-2xl text-slate-900">
          {page.meta?.title || page.frontMatter?.title || page.name}
        </Link>
        <p className="mt-0">
          {page.frontMatter?.description}&nbsp;
          <Link href={page.route} className="text-blue-500 !underline">
            Read more →
          </Link>
        </p>
        {page.frontMatter?.date ? <p className="text-gray-500 text-xs">{page.frontMatter.date}</p> : null}
      </div>
    );
  });
}

# Lagon Blog

---

<BlogIndex />
