{
  "name": "@lagon/example-drizzle-turso",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "generate": "pnpm drizzle-kit generate:sqlite --schema=./schema.ts",
    "push": "pnpm drizzle-kit push:sqlite",
    "lagon-dev": "pnpm generate && pnpm push && lagon dev index.ts -e .env"
  },
  "dependencies": {
    "@libsql/client": "^0.1.3",
    "drizzle-orm": "^0.27.0"
  },
  "devDependencies": {
    "dotenv": "^16.3.1",
    "drizzle-kit": "^0.19.1"
  }
}
